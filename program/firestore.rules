rules_version = '2';

service cloud.firestore {
    match /databases/{database}/documents {
        match /version/1/user/{userId}/{anyDoc=**} {
            allow get: if request.auth != null && request.auth.uid == userId;
        }
    }
}
