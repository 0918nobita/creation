{
  "database": {
    "rules": "database.rules.json"
  },
  "functions": {
    "predeploy": "yarn --cwd \"$RESOURCE_DIR\" run build",
    "source": "functions"
  },
  "emulators": {
    "auth": {
      "port": 9099
    },
    "ui": {
      "enabled": true
    }
  }
}
